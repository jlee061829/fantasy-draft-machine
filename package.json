{
  "name": "fantasy-draft-machine",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.20.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "dev:web": "pnpm --filter @fdm/web dev",
    "dev:socket": "pnpm --filter @fdm/socket-server dev"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "@types/node": "^24.0.0"
  }
}
