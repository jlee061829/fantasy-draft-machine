GITHUB_PAT=

DATABASE_URL=postgresql://fantasy_user:fantasy_password@localhost:5432/fantasy_draft
REDIS_URL=redis://localhost:6379

AUTH_SECRET=
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=

# apps/socket-server: origin(s) allowed to open a Socket.IO connection.
# Comma-separated for multiple origins (e.g. a preview + prod domain later).
SOCKET_CORS_ORIGIN=http://localhost:3000

# apps/web (client-exposed): where the browser harness connects to reach
# apps/socket-server.
NEXT_PUBLIC_SOCKET_SERVER_URL=http://localhost:4000
